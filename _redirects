# Cloudflare Pages Redirects
# Only relative URLs are allowed in Cloudflare Pages

# SEO-friendly redirects for common variations
/home /index.html 301
/home.html /index.html 301

# Note: Removed problematic redirects that were causing redirect loops
# Pages should be accessed directly via their .html URLs
